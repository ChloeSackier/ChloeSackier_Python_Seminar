from visual import *
rod = cylinder (pos=(0,0,0), axis = (0, 0, 1.25), radius = 0.05, opacity = 0.35, color=color.green)
box = box(pos=(0,0,1), length=2, height=2, width=0.01)


#using flux=Qenc/E_0, (and finding Qenc as demonstrated in the visualization,) we can find the flux through
#the area more easily.
